import 'package:flutter/material.dart';

class OfLineView extends StatelessWidget {
  const OfLineView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ofine'),
      ),
    );
  }
}
